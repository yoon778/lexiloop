# 작업 목록

## 상태 표기

- `pending`
- `in_progress`
- `completed`
- `blocked`

## 기획

- [x] `completed` 제품 요구사항 통합
- [x] `completed` Codex·Claude 역할 분리
- [x] `completed` 인수인계 문서 구조 정의
- [x] `completed` 영한 데이터 출처·라이선스 조사
- [x] `completed` Android SDK·최소 버전 결정
- [x] `completed` 화면별 와이어프레임
- [x] `completed` Room 데이터 모델 상세 설계
- [x] `completed` 학습·복습 상태 전이표
- [x] `completed` Gemini 요청·응답 JSON 계약
- [x] `completed` 개인 베타 체크리스트
- [x] `completed` Play Store 출시 전 체크리스트

## 저장소

- [x] `completed` Git 저장소와 GitHub 원격 연결
- [ ] `pending` 별도 `english` 루트 폴더 점유 해제 후 삭제
- [x] `completed` Android 프로젝트 생성과 빌드 문서화

## 구현

- [x] `completed` 정답 정규화·학습·복습 순수 도메인 로직
- [x] `completed` Room 엔티티·DAO·트랜잭션 저장소
- [x] `completed` DataStore 설정 저장소
- [x] `completed` Gemini REST·JSON 검증
- [x] `completed` 알림 예약·재예약
- [x] `completed` Android TextToSpeech
- [x] `completed` ViewModel·UiState·UiEvent 계약 구현
- [ ] `in_progress` 전체 단위·통합 테스트
- [ ] `pending` Claude Compose UI 인계
